MB-EGX parity fixes (desktop/web alignment pass)

db_manager.py
  - FIX: added paper_buy()/paper_sell() wrappers (desktop Paper Trading dialog
    called them but they did not exist - Buy/Sell would have crashed).
    They validate price/shares, apply TRADING_FEE_PCT, seed the paper account
    (100000 EGP default) on first use, guard against overselling, and update
    the paper_account balance.

app_gui.py
  - Matrix now shows Kelly % and Signal Reason columns (appended to the model).
  - Hovering an Action cell shows the full Signal Reason as a tooltip.

index.html
  - Matrix row shows Kelly % inline under the Rank Score with a title tooltip.
  - Action cell shows Signal Reason as a title tooltip on hover.
  - prefers-reduced-motion CSS added (disables animations/transitions).

Verified: py_compile db_manager.py app_gui.py; HTML tokens present.
Not changed: market data JSON payloads (data only); no engine thresholds touched.
